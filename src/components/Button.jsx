import React from "react";

const Button = ({ styles }) => (
  <button
    type="button"
    className="bg-500 bg-yellow-gradient py-2 px-4 rounded-full"
  >
    <strong> Join our Telegram chat for updates </strong>
  </button>
);
export default Button;
